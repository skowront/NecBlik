<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:ZigBee.ZigBeeNet.GUI.ViewModels">
    <DataTemplate x:Key="ZBNetNetworkBriefDataTemplate" DataType="{x:Type viewModels:ZBNetNetworkViewModel}">
        <StackPanel>
            <TextBlock Text="Digi"></TextBlock>
            <TextBlock Text="{Binding Name}"></TextBlock>
            <Button Command="{Binding EditCommand}" Content="Open"></Button>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="ZBNetNetworkDataTemplate" DataType="{x:Type viewModels:ZBNetNetworkViewModel}">
        <StackPanel>
            <TextBlock Text="Digi"></TextBlock>
            <TextBlock Text="{Binding Name}"></TextBlock>
            <TextBlock Text="{Binding Guid}"></TextBlock>
            <Button Command="{Binding EditCommand}" Content="Open"></Button>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>